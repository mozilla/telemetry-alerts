repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.9.0
    hooks:
      - id: ruff
        args: [--fix, --show-fixes]
        files: ^mozbeacon/
      - id: ruff-format
        files: ^mozbeacon/
        exclude: ^mozbeacon/mozbeacon/.*/migrations/
